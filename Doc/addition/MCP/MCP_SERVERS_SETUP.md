# 🔧 MCP Servers Configuration for GPUWorkLib

## ✅ Установленные и работающие серверы

### 1. **sequential-thinking** ✓
- **Назначение**: Решение сложных задач через цепочки рассуждений
- **Использование**: Автоматически используется для сложных вопросов
- **Статус**: ✅ Подключен и работает

### 2. **context7** ✓
- **Назначение**: Расширенная работа с контекстом и документацией
- **Использование**: Поиск документации по CUDA, ROCm, C++, FFT библиотекам
- **Статус**: ✅ Подключен и работает

### 3. **filesystem** ✓
- **Назначение**: Продвинутая работа с файловой системой проекта
- **Путь**: `/home/alex/C++/GPUWorkLib`
- **Использование**: Чтение, запись, поиск файлов в проекте
- **Статус**: ✅ Подключен и работает

### 4. **memory** ✓
- **Назначение**: Постоянная память между сессиями
- **Использование**: Сохранение ключевых находок, настроек, результатов
- **Интеграция**: Работает с `MemoryBank/`
- **Статус**: ✅ Подключен и работает

---

## 🚀 Рекомендуемые серверы для установки

### Для отладки и поиска ошибок:

#### 1. **GitHub MCP Server**
```bash
# Установка GitHub CLI
sudo apt update && sudo apt install -y gh

# Авторизация через SSH
gh auth login
# Выберите: GitHub.com -> SSH -> Your SSH public key

# Добавление MCP сервера
export GITHUB_TOKEN=$(gh auth token)
claude mcp add github -e GITHUB_PERSONAL_ACCESS_TOKEN=$GITHUB_TOKEN -- npx -y @modelcontextprotocol/server-github
```
**Возможности:**
- Поиск issues с похожими ошибками
- Анализ Pull Requests
- Поиск примеров кода на GitHub
- Работа с репозиториями

#### 2. **Brave Search** (для поиска статей и документации)
```bash
# Получить API ключ на: https://brave.com/search/api/
# Бесплатный план: 2000 запросов/месяц

claude mcp add brave-search -e BRAVE_API_KEY=ваш_ключ -- npx -y @modelcontextprotocol/server-brave-search
```
**Возможности:**
- Поиск документации CUDA Toolkit
- Поиск статей по ROCm/HIP
- Примеры оптимизации GPU кода
- Решение специфичных ошибок

---

## 🔍 Для анализа и рефакторинга кода

### 1. **SQLite MCP Server**
```bash
# Создание БД для результатов тестов
touch /home/alex/C++/GPUWorkLib/results.db

claude mcp add sqlite -- npx -y @modelcontextprotocol/server-sqlite --db-path /home/alex/C++/GPUWorkLib/results.db
```
**Использование:**
- Хранение результатов бенчмарков
- Сравнение производительности RTX3060 vs MI100
- Анализ регрессий производительности
- История изменений метрик

### 2. **Puppeteer** (для визуализации)
```bash
claude mcp add puppeteer -- npx -y @modelcontextprotocol/server-puppeteer
```
**Использование:**
- Генерация графиков производительности
- Визуализация результатов FFT
- Создание отчетов с диаграммами

---

## 📊 GPU-специфичные инструменты

### Для NVIDIA RTX 3060 (CUDA)
**Рекомендуемые ресурсы для context7:**
- CUDA Toolkit Documentation
- cuFFT Library Documentation
- NVIDIA Developer Blog
- GPU optimization guides

### Для AMD MI100 (ROCm/HIP)
**Рекомендуемые ресурсы:**
- ROCm Documentation
- hipFFT Library Documentation
- AMD Developer Blog
- HIP Programming Guide

---

## 🛠️ Настройка окружения для отладки

### 1. Переменные окружения
Добавьте в `~/.bashrc`:
```bash
# GPU Development
export CUDA_HOME=/usr/local/cuda
export ROCM_HOME=/opt/rocm
export PATH=$CUDA_HOME/bin:$ROCM_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$ROCM_HOME/lib:$LD_LIBRARY_PATH

# MCP Servers
export GITHUB_TOKEN=$(gh auth token 2>/dev/null || echo "")
```

### 2. Инструменты для профилирования
```bash
# NVIDIA Tools
sudo apt install -y cuda-nsight-systems cuda-nsight-compute

# AMD Tools
sudo apt install -y rocm-smi rocm-profiler

# Общие утилиты
sudo apt install -y valgrind gdb cmake-curses-gui
```

---

## 📝 Проверка установленных серверов

```bash
claude mcp list
```

**Ожидаемый результат:**
```
sequential-thinking: ✓ Connected
context7: ✓ Connected
filesystem: ✓ Connected
memory: ✓ Connected
github: ✓ Connected (после настройки)
brave-search: ✓ Connected (после настройки)
```

---

## 🔧 Управление серверами

### Добавить сервер
```bash
claude mcp add <name> [options] -- <command> [args...]
```

### Удалить сервер
```bash
claude mcp remove <name>
```

### Список серверов
```bash
claude mcp list
```

### Перезапуск сервера
```bash
claude mcp restart <name>
```

---

## 🎯 Особенности для GPUWorkLib проекта

### 1. Структура результатов
- `Results/JSON/` - JSON отчеты (можно импортировать в SQLite)
- `Results/Profiler/` - Профили выполнения
- `MemoryBank/` - Ключевые находки и заметки

### 2. Интеграция с MCP
- `filesystem` MCP имеет доступ только к `/home/alex/C++/GPUWorkLib`
- `memory` MCP синхронизируется с `MemoryBank/AI_SESSION_MEMORY.md`
- `context7` используется для поиска документации CUDA/ROCm

### 3. Рекомендуемый workflow
1. **Разработка**: используйте `filesystem` + `context7` для документации
2. **Отладка**: используйте `sequential-thinking` для сложных багов
3. **Рефакторинг**: используйте `memory` для сохранения паттернов
4. **Поиск решений**: используйте `github` + `brave-search`

---

## 📚 Дополнительные ресурсы

### Официальная документация MCP
- https://modelcontextprotocol.io/

### Список доступных серверов
- https://github.com/modelcontextprotocol/servers

### Создание собственных серверов
- https://modelcontextprotocol.io/docs/building-servers

---

*Создано: 2026-02-05*
*Проект: GPUWorkLib - Multi-GPU FFT/IFFT Benchmark*
*GPU: NVIDIA RTX 3060 + AMD MI100*
