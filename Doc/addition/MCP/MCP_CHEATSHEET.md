# 🚀 MCP Servers - Быстрая шпаргалка

## ⚡ Быстрый старт

### Установка всех серверов одной командой
```bash
cd /home/alex/C++/GPUWorkLib/Doc
./install_mcp_servers.sh
```

---

## 📋 Основные команды

### Посмотреть список серверов
```bash
claude mcp list
```

### Добавить сервер
```bash
claude mcp add <name> -- <command> [args...]
```

### Удалить сервер
```bash
claude mcp remove <name>
```

### Перезапустить сервер
```bash
claude mcp restart <name>
```

---

## 🔧 Быстрая настройка GitHub

```bash
# 1. Установка (если нужно)
sudo apt install -y gh

# 2. Авторизация через SSH
gh auth login
# → Выбрать: GitHub.com
# → Выбрать: SSH
# → Выбрать: Your SSH public key

# 3. Добавление MCP сервера
export GITHUB_TOKEN=$(gh auth token)
claude mcp add github -e GITHUB_PERSONAL_ACCESS_TOKEN=$GITHUB_TOKEN -- npx -y @modelcontextprotocol/server-github

# 4. Проверка
claude mcp list | grep github
```

---

## 🔍 Быстрая настройка Brave Search

```bash
# 1. Получить API ключ
# → Открыть: https://brave.com/search/api/
# → Зарегистрироваться (бесплатно 2000 запросов/месяц)
# → Скопировать API ключ

# 2. Добавить сервер
claude mcp add brave-search -e BRAVE_API_KEY=ваш_ключ -- npx -y @modelcontextprotocol/server-brave-search

# 3. Проверка
claude mcp list | grep brave
```

---

## 💾 Использование в проекте

### Для отладки CUDA/ROCm ошибок
```bash
# Используйте sequential-thinking для сложных багов
# Он подключен автоматически и активируется при необходимости
```

### Поиск документации
```bash
# context7 - автоматически ищет документацию
# Просто спросите: "Как работает cuFFT batched transforms?"
```

### Работа с файлами проекта
```bash
# filesystem - автоматически работает с вашим проектом
# Путь: /home/alex/C++/GPUWorkLib
```

### Сохранение результатов
```bash
# memory - сохраняет ключевые находки между сессиями
# Автоматически синхронизируется с MemoryBank/
```

---

## 🎯 Примеры использования

### 1. Отладка ошибки компиляции CUDA
```
Я: У меня ошибка компиляции в файле fft_kernel.cu
Кодо: [использует filesystem для чтения файла]
Кодо: [использует context7 для поиска решения]
Кодо: [использует sequential-thinking для анализа]
```

### 2. Поиск примеров оптимизации
```
Я: Найди примеры оптимизации cuFFT для batch processing
Кодо: [использует brave-search для статей]
Кодо: [использует github MCP для примеров кода]
Кодо: [использует context7 для документации]
```

### 3. Анализ производительности
```
Я: Сравни результаты RTX3060 и MI100
Кодо: [использует sqlite для запроса результатов]
Кодо: [анализирует данные из results.db]
Кодо: [создает сравнительный отчет]
```

### 4. Рефакторинг кода
```
Я: Нужно отрефакторить модуль search_maxima
Кодо: [использует filesystem для чтения]
Кодо: [использует memory для сохранения паттернов]
Кодо: [предлагает улучшения]
```

---

## 🐛 Решение проблем

### Сервер не подключается
```bash
# Проверить статус
claude mcp list

# Перезапустить
claude mcp restart <name>

# Удалить и добавить заново
claude mcp remove <name>
claude mcp add <name> -- <command> [args...]
```

### GitHub токен не работает
```bash
# Проверить авторизацию
gh auth status

# Переавторизоваться
gh auth logout
gh auth login

# Обновить токен в MCP
claude mcp remove github
export GITHUB_TOKEN=$(gh auth token)
claude mcp add github -e GITHUB_PERSONAL_ACCESS_TOKEN=$GITHUB_TOKEN -- npx -y @modelcontextprotocol/server-github
```

### Brave Search не работает
```bash
# Проверить ключ
echo $BRAVE_API_KEY

# Переустановить с новым ключом
claude mcp remove brave-search
claude mcp add brave-search -e BRAVE_API_KEY=новый_ключ -- npx -y @modelcontextprotocol/server-brave-search
```

---

## 📊 Мониторинг использования

### Проверка работы серверов
```bash
# Полная проверка здоровья
claude mcp list

# Должно быть:
# ✓ Connected - сервер работает
# ✗ Failed - сервер не работает (проверьте настройки)
```

### Просмотр логов (если нужно)
```bash
# Логи Claude Code
~/.claude/logs/

# Системные логи npm (для отладки MCP)
~/.npm/_logs/
```

---

## 🎓 Полезные ссылки

- **Документация MCP**: https://modelcontextprotocol.io/
- **Список серверов**: https://github.com/modelcontextprotocol/servers
- **CUDA Docs**: https://docs.nvidia.com/cuda/
- **ROCm Docs**: https://rocm.docs.amd.com/
- **cuFFT Guide**: https://docs.nvidia.com/cuda/cufft/
- **hipFFT Guide**: https://rocm.docs.amd.com/projects/hipFFT/

---

## 🔑 Переменные окружения

Добавьте в `~/.bashrc` для постоянной работы:

```bash
# GitHub
export GITHUB_TOKEN=$(gh auth token 2>/dev/null || echo "")

# Brave Search (после получения ключа)
export BRAVE_API_KEY="ваш_ключ_здесь"

# GPU Paths
export CUDA_HOME=/usr/local/cuda
export ROCM_HOME=/opt/rocm
export PATH=$CUDA_HOME/bin:$ROCM_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$ROCM_HOME/lib:$LD_LIBRARY_PATH
```

Применить изменения:
```bash
source ~/.bashrc
```

---

## ✅ Текущий статус

### ✓ Подключено и работает:
- sequential-thinking
- context7
- filesystem
- memory

### ⏳ Рекомендуется добавить:
- github (для поиска решений)
- brave-search (для документации)
- sqlite (для результатов тестов)
- git (расширенная работа с git)
- fetch (загрузка примеров)

---

*Последнее обновление: 2026-02-05*
*Проект: GPUWorkLib*
*GPU: RTX 3060 + AMD MI100*
